apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-config
  namespace: roboshop
data:
   my.cnf: |
       [mysqld]
        innodb_redo_log_capacity = 2G

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql
  namespace: roboshop
   # these are replica set labels
  labels:
    project: roboshop
    component: mysql
    tier: db

spec:
  replicas: 1
  selector:
    # these labels are used to select pods
    matchLabels:
       project: roboshop
       component: mysql
       tier: db
  template:
    metadata:
    ## pod labels
      labels:
       project: roboshop
       component: mysql
       tier: db
    spec:
      containers:
      - name: redis
        image: venkatsai12345/mysql:v1
        imagePullPolicy: Always
        # readinessProbe:
        #    tcpSocket:
        #       port: 3306
        #    initialDelaySeconds: 20
        #    periodSeconds: 10
        # livenessProbe:
        #     tcpSocket:
        #       port: 3306
        #     initialDelaySeconds: 5
        #     periodSeconds: 5
        volumeMounts:
        - name: mysql-config
          mountPath: /etc/mysql/conf.d/my.cnf

      volumes:
        - name: mysql-config
          configMap:
            name: mysql-config

---
apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: roboshop
spec:
  selector:
    project: roboshop
    component: mysql
    tier: db

  ports:
    - protocol: TCP
      port: 3306 ## service port
      targetPort: 3306 ## container port

